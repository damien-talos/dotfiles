# Don't let ripgrep vomit really long lines to my terminal, and show a preview.
--max-columns=150
--max-columns-preview

# Add my 'web' type (allows `rg --type web 'function' ` or `rg -tweb 'function' `)
--type-add
web:*.{htm,html,css,js,jsx,ts,tsx,scss}

# Search hidden files / directories (e.g. dotfiles) by default
# --hidden
# --unrestricted

# Set the colors.
# --colors=line:none
# --colors=line:style:bold

# Because who cares about case!?
--smart-case

# --stats

# Respect .gitignore files even if not starting in a git repo
--no-require-git 
